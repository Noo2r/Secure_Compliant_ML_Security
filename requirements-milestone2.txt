# Milestone 2 — Model Development & Risk Analysis
# Installed incrementally as modules are built. Module 1 (this PR) only
# exercises the packages marked [M1]; the rest are declared upfront since
# they are fixed by the project's official tech-stack requirement and will
# be exercised by later modules in this same milestone.

# --- Core data handling [M1] ---
pandas>=2.2
numpy>=1.26
PyYAML>=6.0

# --- Milestone 1 integration / secured-dataset regeneration [M1] ---
Faker>=24.0
cryptography>=42.0

# --- Testing [M1] ---
pytest>=8.0

# --- Model development (Module 2+) ---
scikit-learn>=1.4
joblib>=1.3   # persisting fitted Module 2 pipeline objects for inference reuse
xgboost>=2.0
lightgbm>=4.3
tensorflow>=2.16
tensorflow-privacy>=0.9

# --- Hyperparameter optimization (Module 2+) ---
optuna>=3.6
tabulate>=0.9   # DataFrame.to_markdown() for report generation
scipy>=1.11     # Wilcoxon signed-rank test (model comparison)

# --- Explainability (Module 6+) ---
shap>=0.45
lime>=0.2.0.1

# --- Fairness (Module 5+) ---
fairlearn>=0.10

# --- Experiment tracking / registry (Module 8+) ---
mlflow>=2.12
azure-ai-ml>=1.16
azure-identity>=1.16

# --- Visualization (evaluation/reports) ---
matplotlib>=3.8
seaborn>=0.13
